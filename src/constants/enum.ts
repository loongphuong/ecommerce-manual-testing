export enum Environment {
  local = 'local',
  production = 'production',
  dev = 'dev',
  test = 'test',
}

export enum RoleType {
  USER = 'user',
  ADMIN = 'admin',
  SELLER = 'seller',
}

export enum LanguageCode {
  EN = 'en',
  VI = 'vi',
}
