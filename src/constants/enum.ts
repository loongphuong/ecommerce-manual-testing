export enum Environment {
  local = 'local',
  production = 'production',
  dev = 'dev',
  test = 'test',
}

export enum RoleType {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export enum LanguageCode {
  EN = 'en',
  VI = 'vi',
}
